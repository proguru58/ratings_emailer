= simple_form_for(:rating_email, url: rating_email_send_email_path) do |f|
  = f.error_notification

  .form-inputs
    = f.input :emails
    = f.input :html, input_html: {class: "codemirror"}, as: :text

  .form-actions
    = f.button :submit, class: 'btn-primary'
